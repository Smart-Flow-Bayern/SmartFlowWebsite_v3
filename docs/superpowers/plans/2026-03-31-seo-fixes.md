# SEO Fixes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fix all Critical and High severity SEO issues identified in audit before pushing to GitHub.

**Architecture:** Direct edits to static HTML files and config files. No build step. Changes to `.min.css`/`.min.js` files are already done. Focus on meta tags, structured data, sitemap, and robots.txt.

**Tech Stack:** Static HTML, JSON-LD structured data, XML sitemap

---

## File Map

| File | Changes |
|------|---------|
| `index.html` | Fix OG image URL, title, meta description, LocalBusiness schema, add font preload, add og:locale, add geo meta, add meta author |
| `en/index.html` | Fix OG image URL, title, meta description, LocalBusiness schema, fix EN schema URL, add font preload, add og:locale, add geo meta, add meta author |
| `sitemap.xml` | Remove 6 legal pages, add x-default hreflang |
| `robots.txt` | Add Disallow rules for legal pages |
| `404.html` | Add noindex meta, switch to min CSS/JS |
| `en/404.html` | Add noindex meta, switch to min CSS/JS |
| `impressum.html` | Add canonical, switch to min CSS/JS, fix aria-label |
| `datenschutz.html` | Add canonical, switch to min CSS/JS, fix aria-label |
| `agb.html` | Add canonical, switch to min CSS/JS, fix aria-label |
| `en/legal-notice.html` | Add canonical, switch to min CSS/JS |
| `en/privacy-policy.html` | Add canonical, switch to min CSS/JS |
| `en/terms.html` | Add canonical, switch to min CSS/JS |

---

### Task 1: Fix OG image URLs on both main pages (CRITICAL)

**Files:**
- Modify: `index.html:25,31`
- Modify: `en/index.html:25,31`

- [ ] **Step 1: Fix DE OG/Twitter image to absolute URL**

In `index.html`, change:
```html
<meta property="og:image" content="assets/og-image.jpg" />
```
to:
```html
<meta property="og:image" content="https://smartflow.bayern/assets/og-image.jpg" />
```

Same for `twitter:image`:
```html
<meta name="twitter:image" content="https://smartflow.bayern/assets/og-image.jpg" />
```

- [ ] **Step 2: Fix EN OG/Twitter image to absolute URL**

In `en/index.html`, change:
```html
<meta property="og:image" content="../assets/og-image.jpg" />
```
to:
```html
<meta property="og:image" content="https://smartflow.bayern/assets/og-image.jpg" />
```

Same for `twitter:image`:
```html
<meta name="twitter:image" content="https://smartflow.bayern/assets/og-image.jpg" />
```

- [ ] **Step 3: Verify** — grep for relative og-image references:
```bash
grep -n 'og-image' index.html en/index.html | grep -v 'https://'
```
Expected: no output (all absolute)

---

### Task 2: Clean up sitemap.xml (CRITICAL)

**Files:**
- Modify: `sitemap.xml`

- [ ] **Step 1: Remove all 6 legal page URL blocks**

Remove the `<url>` blocks for:
- `impressum.html`
- `datenschutz.html`
- `agb.html`
- `en/legal-notice.html`
- `en/privacy-policy.html`
- `en/terms.html`

- [ ] **Step 2: Add x-default hreflang to remaining entries**

For the DE main page entry, add:
```xml
<xhtml:link rel="alternate" hreflang="x-default" href="https://smartflow.bayern/"/>
```

For the EN main page entry, add:
```xml
<xhtml:link rel="alternate" hreflang="x-default" href="https://smartflow.bayern/"/>
```

- [ ] **Step 3: Final sitemap should contain exactly 2 `<url>` blocks**

```xml
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
        xmlns:xhtml="http://www.w3.org/1999/xhtml">
  <url>
    <loc>https://smartflow.bayern/</loc>
    <xhtml:link rel="alternate" hreflang="de" href="https://smartflow.bayern/"/>
    <xhtml:link rel="alternate" hreflang="en" href="https://smartflow.bayern/en/"/>
    <xhtml:link rel="alternate" hreflang="x-default" href="https://smartflow.bayern/"/>
    <lastmod>2026-03-31</lastmod>
    <changefreq>monthly</changefreq>
    <priority>1.0</priority>
  </url>
  <url>
    <loc>https://smartflow.bayern/en/</loc>
    <xhtml:link rel="alternate" hreflang="de" href="https://smartflow.bayern/"/>
    <xhtml:link rel="alternate" hreflang="en" href="https://smartflow.bayern/en/"/>
    <xhtml:link rel="alternate" hreflang="x-default" href="https://smartflow.bayern/"/>
    <lastmod>2026-03-31</lastmod>
    <changefreq>monthly</changefreq>
    <priority>1.0</priority>
  </url>
</urlset>
```

---

### Task 3: Update robots.txt (HIGH)

**Files:**
- Modify: `robots.txt`

- [ ] **Step 1: Add Disallow rules for legal pages**

Replace full file content with:
```
# robots.txt for smartflow.bayern
User-agent: *
Allow: /

# Legal pages (noindex)
Disallow: /impressum.html
Disallow: /datenschutz.html
Disallow: /agb.html
Disallow: /en/legal-notice.html
Disallow: /en/privacy-policy.html
Disallow: /en/terms.html

# Sitemap
Sitemap: https://smartflow.bayern/sitemap.xml
```

---

### Task 4: Optimize title, meta description, and add SEO meta tags on DE homepage (HIGH)

**Files:**
- Modify: `index.html:8-9` (title + description)
- Modify: `index.html` `<head>` section (add new meta tags)

- [ ] **Step 1: Update title tag**

Change:
```html
<title>SmartFlow | Prozesse optimieren. Messbar.</title>
```
to:
```html
<title>SmartFlow Regensburg | Prozessoptimierung & Digitalisierung</title>
```

- [ ] **Step 2: Update meta description**

Change:
```html
<meta name="description" content="SmartFlow – Prozessoptimierung, Digitalisierung und Automatisierung für Ihr Unternehmen. Messbare Ergebnisse." />
```
to:
```html
<meta name="description" content="SmartFlow in Regensburg – Prozessoptimierung, Digitalisierung und Automatisierung für KMU in Bayern. Kostenloses Erstgespräch buchen." />
```

- [ ] **Step 3: Add og:locale, geo meta tags, meta author, and font preload**

Add after the Twitter Card meta tags and before `<!-- Stylesheet -->`:
```html
<meta property="og:locale" content="de_DE" />
<meta name="geo.region" content="DE-BY" />
<meta name="geo.placename" content="Regensburg" />
<meta name="geo.position" content="49.0169;12.0952" />
<meta name="ICBM" content="49.0169, 12.0952" />
<meta name="author" content="Leon Heinrich, SmartFlow" />

<!-- Font preload -->
<link rel="preload" as="font" type="font/woff2" href="assets/fonts/manrope.woff2" crossorigin />
```

- [ ] **Step 4: Update OG title to match new title**

Change:
```html
<meta property="og:title" content="SmartFlow | Prozesse optimieren. Messbar." />
```
to:
```html
<meta property="og:title" content="SmartFlow Regensburg | Prozessoptimierung & Digitalisierung" />
```

Same for `twitter:title`.

---

### Task 5: Optimize title, meta description, and add SEO meta tags on EN homepage (HIGH)

**Files:**
- Modify: `en/index.html:8-9` (title + description)
- Modify: `en/index.html` `<head>` section

- [ ] **Step 1: Update title tag**

Change:
```html
<title>SmartFlow | Optimize processes. Measurably.</title>
```
to:
```html
<title>SmartFlow Regensburg | Process Optimization & Digitization</title>
```

- [ ] **Step 2: Update meta description**

Change:
```html
<meta name="description" content="SmartFlow – Process optimization, digitization, and automation for your business. Measurable results." />
```
to:
```html
<meta name="description" content="SmartFlow in Regensburg – Process optimization, digitization, and automation for SMEs in Bavaria. Book a free consultation." />
```

- [ ] **Step 3: Add og:locale, geo meta tags, meta author, and font preload**

Add after the Twitter Card meta tags and before `<!-- Stylesheet -->`:
```html
<meta property="og:locale" content="en_US" />
<meta name="geo.region" content="DE-BY" />
<meta name="geo.placename" content="Regensburg" />
<meta name="geo.position" content="49.0169;12.0952" />
<meta name="ICBM" content="49.0169, 12.0952" />
<meta name="author" content="Leon Heinrich, SmartFlow" />

<!-- Font preload -->
<link rel="preload" as="font" type="font/woff2" href="../assets/fonts/manrope.woff2" crossorigin />
```

- [ ] **Step 4: Update OG title and Twitter title to match**

Change both `og:title` and `twitter:title` to:
```
SmartFlow Regensburg | Process Optimization & Digitization
```

- [ ] **Step 5: Fix EN LocalBusiness schema URL**

In the LocalBusiness JSON-LD, change:
```json
"url": "https://smartflow.bayern/en/"
```
to:
```json
"url": "https://smartflow.bayern/"
```
(The business entity URL is always the primary DE homepage.)

---

### Task 6: Expand LocalBusiness structured data on both pages (HIGH)

**Files:**
- Modify: `index.html` LocalBusiness JSON-LD block
- Modify: `en/index.html` LocalBusiness JSON-LD block

- [ ] **Step 1: Update DE LocalBusiness schema**

Replace the existing `LocalBusiness` JSON-LD with:
```json
{
  "@context": "https://schema.org",
  "@type": "ProfessionalService",
  "name": "SmartFlow",
  "address": {
    "@type": "PostalAddress",
    "streetAddress": "Arnulfsplatz 2",
    "postalCode": "93047",
    "addressLocality": "Regensburg",
    "addressCountry": "DE"
  },
  "email": "info@smartflow.bayern",
  "telephone": "+49 173 4487550",
  "url": "https://smartflow.bayern/",
  "image": "https://smartflow.bayern/assets/og-image.jpg",
  "description": "Prozessoptimierung, Digitalisierung und Automatisierung für KMU in Regensburg und Bayern.",
  "areaServed": {
    "@type": "State",
    "name": "Bayern"
  },
  "openingHoursSpecification": {
    "@type": "OpeningHoursSpecification",
    "dayOfWeek": ["Monday", "Tuesday", "Wednesday", "Thursday", "Friday"],
    "opens": "09:00",
    "closes": "18:00"
  }
}
```

- [ ] **Step 2: Update EN LocalBusiness schema**

Use the exact same schema as Step 1 (same business entity, same URL pointing to DE homepage).

---

### Task 7: Fix 404 pages (HIGH)

**Files:**
- Modify: `404.html`
- Modify: `en/404.html`

- [ ] **Step 1: Add noindex to DE 404 page**

Add inside `<head>`:
```html
<meta name="robots" content="noindex, nofollow">
```

- [ ] **Step 2: Switch DE 404 to minified assets**

Change:
```html
<link rel="stylesheet" href="css/styles.css">
```
to:
```html
<link rel="stylesheet" href="css/styles.min.css">
```

Add before `</body>`:
```html
<script src="js/main.min.js"></script>
```

- [ ] **Step 3: Add noindex to EN 404 page**

Add inside `<head>`:
```html
<meta name="robots" content="noindex, nofollow">
```

- [ ] **Step 4: Switch EN 404 to minified assets**

Change:
```html
<link rel="stylesheet" href="../css/styles.css">
```
to:
```html
<link rel="stylesheet" href="../css/styles.min.css">
```

Add before `</body>`:
```html
<script src="../js/main.min.js"></script>
```

---

### Task 8: Fix all 6 legal pages — add canonical, switch to min assets, fix aria-labels (CRITICAL + HIGH)

**Files:**
- Modify: `impressum.html`
- Modify: `datenschutz.html`
- Modify: `agb.html`
- Modify: `en/legal-notice.html`
- Modify: `en/privacy-policy.html`
- Modify: `en/terms.html`

- [ ] **Step 1: Fix `impressum.html`**

Add in `<head>`:
```html
<link rel="canonical" href="https://smartflow.bayern/impressum.html" />
```

Change CSS reference:
```html
<link rel="stylesheet" href="css/styles.css">
```
to:
```html
<link rel="stylesheet" href="css/styles.min.css">
```

Change JS reference:
```html
<script src="js/main.js"></script>
```
to:
```html
<script src="js/main.min.js"></script>
```

Fix aria-label (if present):
```
aria-label="Menue oeffnen"  →  aria-label="Menü öffnen"
```

- [ ] **Step 2: Fix `datenschutz.html`**

Add in `<head>`:
```html
<link rel="canonical" href="https://smartflow.bayern/datenschutz.html" />
```

Same CSS/JS and aria-label fixes as Step 1.

- [ ] **Step 3: Fix `agb.html`**

Add in `<head>`:
```html
<link rel="canonical" href="https://smartflow.bayern/agb.html" />
```

Same CSS/JS and aria-label fixes as Step 1.

- [ ] **Step 4: Fix `en/legal-notice.html`**

Add in `<head>`:
```html
<link rel="canonical" href="https://smartflow.bayern/en/legal-notice.html" />
```

Change CSS reference:
```html
<link rel="stylesheet" href="../css/styles.css">
```
to:
```html
<link rel="stylesheet" href="../css/styles.min.css">
```

Change JS reference:
```html
<script src="../js/main.js"></script>
```
to:
```html
<script src="../js/main.min.js"></script>
```

- [ ] **Step 5: Fix `en/privacy-policy.html`**

Add in `<head>`:
```html
<link rel="canonical" href="https://smartflow.bayern/en/privacy-policy.html" />
```

Same CSS/JS fixes as Step 4.

- [ ] **Step 6: Fix `en/terms.html`**

Add in `<head>`:
```html
<link rel="canonical" href="https://smartflow.bayern/en/terms.html" />
```

Same CSS/JS fixes as Step 4.

---

### Task 9: Commit all SEO fixes

- [ ] **Step 1: Stage and commit**

```bash
git add index.html en/index.html sitemap.xml robots.txt 404.html en/404.html impressum.html datenschutz.html agb.html en/legal-notice.html en/privacy-policy.html en/terms.html
git commit -m "fix: SEO — absolute OG URLs, expanded schema, optimized titles, cleaned sitemap, added canonicals"
```
